apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: disaster-response

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      port: 8081
      targetPort: 8081

  type: ClusterIP