apiVersion: v1
kind: Service

metadata:
  name: analytics-service
  namespace: disaster-response

spec:
  selector:
    app: analytics-service

  ports:
    - name: http
      port: 8082
      targetPort: 8082

  type: ClusterIP