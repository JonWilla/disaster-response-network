spring:
  application:
    name: analytics-service

  datasource:
    url: ${ANALYTICS_DB_URL:jdbc:postgresql://localhost:5433/analytics_service}
    username: ${DB_USERNAME:disaster_user}
    password: ${DB_PASSWORD:disaster_password}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}

    consumer:
      group-id: analytics-service-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JacksonJsonDeserializer

      properties:
        spring.json.trusted.packages: "*"
        spring.json.value.default.type: com.jonwilla.analytics.event.IncidentCreatedEvent
        spring.json.use.type.headers: false

server:
  port: ${SERVER_PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info