apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: disaster-response

spec:
  selector:
    app: api-gateway

  ports:
    - name: http
      port: 8090
      targetPort: 8090

  type: ClusterIP